name: News Crawler Workflowon:매시간 0분마다 실행schedule:- cron: '0 * * * *'수동 실행을 위한 설정 (테스트용)workflow_dispatch:jobs:crawl_and_update:runs-on: ubuntu-lateststeps:
  - name: 저장소 체크아웃
    uses: actions/checkout@v4

  - name: Python 환경 설정
    uses: actions/setup-python@v5
    with:
      python-version: '3.10'

  - name: 필요한 라이브러리 설치
    run: pip install requests beautifulsoup4

  - name: 뉴스 크롤링 스크립트 실행
    run: python crawler.py

  - name: 변경된 파일 커밋 및 푸시
    run: |
      git config --global user.name 'github-actions[bot]'
      git config --global user.email 'github-actions[bot]@users.noreply.github.com'
      git add news_data.json
      git commit -m 'Updated news data via GitHub Actions' || true
      git push

